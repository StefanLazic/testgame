// The single source of truth for the game version.
//
// Bump this on every pull request (see docs/AGENTS.md) and keep the "version"
// field in package.json identical — tests/unit/version.test.js enforces it.
export const VERSION = '1.1.0';
