// ---------------------------------------------------------------------------
// The game version. This is the single source of truth: `package.json` mirrors
// it and tests/unit/version.test.js keeps the two in sync.
//
// Bump it (semver: MAJOR.MINOR.PATCH) in every pull request — see docs/AGENTS.md.
// ---------------------------------------------------------------------------

export const VERSION = '1.3.0';
