{
  "name": "mimi-chan-defense",
  "version": "1.1.0",
  "private": true,
  "description": "3D browser tower defense. Static files only — this manifest exists purely so the tests can be run with plain node.",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "node --test tests/unit/*.test.js",
    "test:browser": "node --test --test-timeout=240000 tests/browser/*.test.js",
    "test:all": "npm run test && npm run test:browser"
  }
}
